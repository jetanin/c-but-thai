นำเข้าไลบรารี <stdio.h>

ตัวแปรประเภทจำนวนเต็ม add(int a, ตัวแปรประเภทจำนวนเต็ม b) { คืนค่า a บวก b; }

ตัวแปรประเภทจำนวนเต็ม ฟังก์ชั่นสำหรับการทำงานหลัก {
  printf("hello world!");
  ตัวแปรประเภทจำนวนเต็ม a เท่ากับ 1, b เท่ากับ 2;
  printf("%d", add(a, b));
  คืนค่า 0;
}
