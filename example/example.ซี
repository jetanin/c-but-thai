นำเข้าไลบรารี <stdio.h>

ตัวแปรประเภทจำนวนเต็ม add(int a, ตัวแปรประเภทจำนวนเต็ม b) { คืนค่า a เครื่องหมายบวก b; }

ตัวแปรประเภทจำนวนเต็ม ฟังก์ชั่นสำหรับการทำงานหลัก {
  printf("hello world!");
  ตัวแปรประเภทจำนวนเต็ม a เครื่องหมายเท่ากับ 1, b เครื่องหมายเท่ากับ 2;
  printf("%d", add(a, b));
  คืนค่า 0;
}
