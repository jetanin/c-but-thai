นำเข้าไลบรารี <stdio.h>

ตัวแปรประเภทจำนวนเต็ม add(int a, ตัวแปรประเภทจำนวนเต็ม b) { คืนค่า a + b; }

ตัวแปรประเภทจำนวนเต็ม ฟังก์ชั่นสำหรับการทำงานหลัก {
  printf("hello world!");
  ตัวแปรประเภทจำนวนเต็ม a = 1, b = 2;
  printf("%d", add(a, b));
  คืนค่า 0;
}
